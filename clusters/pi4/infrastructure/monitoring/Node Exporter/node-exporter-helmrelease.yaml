apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: node-exporter
  namespace: vm
spec:
  releaseName: node-exporter
  chart:
    spec:
      chart: prometheus-node-exporter
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
      version: "4.42.0"
  interval: 5m
  values:
  # Тут можна додати кастомні налаштування для node-exporter
  # наприклад:
  # service:
  #   type: ClusterIP
  #   port: 9100
